import { useParams } from "react-router-dom";

export const UserPage : React.FC = () => {
    const { id } = useParams();
    return (
        <div>
            Bober
        </div>
    )
}
export default UserPage;